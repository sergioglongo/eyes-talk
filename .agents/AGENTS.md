# Custom Project Rules & Instructions

- **Git Push Policy**: Do NOT automatically commit or push code changes to GitHub/git repositories (`dev` or `main`). Only perform `git commit` and `git push` operations when the user explicitly requests to upload or push the code.
